# --- Core Frameworks ---
torch>=2.4.0
torchvision
accelerate>=0.33.0
transformers>=4.49.0  # CRITICAL: Qwen2.5-VL requires >=4.49.0
diffusers>=0.29.0     # For SDXL-Turbo

# --- Model Specific Dependencies ---
# Required for Qwen2.5-VL image processing
qwen-vl-utils
bitsandbytes          # For 4-bit loading in Reasoner

# Required for Florence-2 (AutoLabeler)
timm                  # Florence-2 vision backbone relies on this
einops                # Florence-2 tensor operations

# --- Clustering & Math ---
scikit-learn
umap-learn
hdbscan
numpy

# --- Utilities ---
tqdm                  # Used in almost every loop (Diagnoser, Generator, Labeler)
pillow
opencv-python
